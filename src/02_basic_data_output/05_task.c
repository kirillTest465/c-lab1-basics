#include <stdio.h> // Подключаем библиотеку ввода/вывода

int main()
{
    // Объявляем и инициализируем переменные
    int a = 200;
    int b = 1217;
    int c = 999;

    // Выводим значение переменых каждую с новой строки
    printf("a = %d\n", a);
    printf("b = %d\n", b);
    printf("c = %d\n", c);

    return 0;
}