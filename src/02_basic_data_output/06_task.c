#include <stdio.h> // Подключаем библиотеку для ввода/вывода

int main()
{
    // Объявляем и инициализируем переменные
    double x_1 = 325.025;
    double x_2 = 102.300;

    // Выводим значение переменых с поясняющим текстом
    printf("x1 = %.3f\n", x_1);
    printf("x2 = %.3f\n", x_2);

    return 0;
}
