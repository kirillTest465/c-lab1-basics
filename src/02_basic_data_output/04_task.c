#include <stdio.h> // Подключаем библиотеку ввода/вывода

int main()
{
    // Объявляем и инициализиреум переменные
    int a = 100;
    int b = 230;
    int c = 1000;

    // Выводим значение переменных с одну строку
    printf("a = %d, b= %d, c = %d", a, b, c);

    return 0;
}