/*
int *p;             указатель не инициализирован, хранит мусорный адрес
printf("%d", *p);   разыменование

Что произойдёт?
Указатель p содержит случайный адрес (то, что осталось в памяти от предыдущих данных).
Программа попытается прочитать значение по этому случайному адресу. Это неопределённое
поведение (undefined behavior). Возможные последствия

1)Программа выведет какое-то «мусорное» значение (если адрес случайно оказался допустимым и
доступным для чтения).

2)Программа мгновенно завершится с ошибкой сегментации (segmentation fault), если адрес не
принадлежит программе или защищён от чтения.

3)Может произойти что угодно: от неправильных вычислений до краха системы (в теории).

Вывод: Никогда нельзя разыменовывать неинициализированный указатель.



Запись значения по адресу неинициализированного указателя
int *p;    // мусорный адрес
*p = 42;   // попытка записать 42 по случайному адресу

Что произойдёт?
Это тоже неопределённое поведение. Программа попытается записать число 42 по случайному
адресу. Последствия:

1)Если адрес случайно указывает на доступную область памяти (например, на другую переменную),
вы молча испортите её значение – трудноуловимая ошибка.

2)Если адрес недоступен для записи (защищённая область, или вне адресного пространства
программы), произойдёт ошибка сегментации.

3)Возможны любые побочные эффекты, включая порчу стека, данных кучи и т.д.
*/
