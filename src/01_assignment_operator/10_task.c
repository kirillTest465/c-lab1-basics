int main()
{
    // Объявляем и инициализируем переменные
    double base = 8;   // Основание треугольника
    double height = 4; // Высота треугольника
    double area;       // Переменная для площади треугольника

    // Расчет площади треугольнка
    area = 0.5 * base * height;
}